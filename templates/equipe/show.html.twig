{% extends 'base.html.twig' %}

{% block title %}Equipe{% endblock %}

{% block body %}
    <h1>Equipe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ equipe.id }}</td>
            </tr>
            <tr>
                <th>NOM</th>
                <td>{{ equipe.NOM }}</td>
            </tr>
            <tr>
                <th>PRENOM</th>
                <td>{{ equipe.PRENOM }}</td>
            </tr>
            <tr>
                <th>DESCRIPTION</th>
                <td>{{ equipe.DESCRIPTION }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_creation') }}" class="backlink">back to list</a>

    <a href="{{ path('app_equipe_edit', {'id': equipe.id}) }}">edit</a>

    {{ include('equipe/_delete_form.html.twig') }}
{% endblock %}
