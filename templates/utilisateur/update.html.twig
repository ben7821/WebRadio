{% extends 'base.html.twig' %}

{% block title %}Modifier le mot de passe
{% endblock %}
{% block body %}

<h1 class="display-full">Modifier le mot de passe</h1>
{{ form_start(form, {'attr': {'id': 'formentity'}}) }}
    {{ form_errors(form) }}
    {# {% if not admin %}
        <div>
            {{ form_label(form.password, 'Mot de passe actuel') }}
            {{ form_widget(form.password, {'attr': {'class': 'form-control'}}) }}
            {{ form_errors(form.password) }}
        </div>
    {% endif %} #}

    {# <div>
        {{ form_label(form.newPassword.first, 'Nouveau mot de passe') }}
        {{ form_widget(form.newPassword.first, {'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.newPassword.first) }}
    </div> #}

    {# <div>
        {{ form_label(form.newPassword.second, 'Confirmer le mot de passe') }}
        {{ form_widget(form.newPassword.second, {'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.newPassword.second) }}
    </div> #}

    {{ form_widget(form) }}
    <button class="btn">Valider</button>
    {# <button type="submit" class="btn">Valider</button> #}
{{ form_end(form) }}
{% endblock %}