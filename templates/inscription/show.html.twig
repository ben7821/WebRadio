{% extends 'base.html.twig' %}

{% block title %}Inscription emission
{% endblock %}
{% block body %}
	<h1>{{ emission.NOMLONG}} / n° : {{ inscription.ID}}</h1>
	<p>{{ inscription.DATE ? inscription.DATE|date('Y-m-d') : '' }}</p>
	<p>Liste des participant : <p/>
		<tbody>
			<table class="table">
				{% for participant in participants %}
					<tr>
						<td>{{ participant.PRENOM }}</td>
						<td>{{ participant.NOM }}</td>
						<td>{{ participant.TEL }}</td>
						<td>{{ participant.MAIL }}</td>
					</tr>
				{% endfor %}
			</table>
		</tbody>

		<a href="{{ path('app_creation') }}">back to list</a>
{% endblock %}
