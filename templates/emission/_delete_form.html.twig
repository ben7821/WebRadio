<form method="post" action="{{ path('app_emission_delete', {'ID': emission.ID}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ emission.ID) }}">
    <button class="btn">Delete</button>
</form>
